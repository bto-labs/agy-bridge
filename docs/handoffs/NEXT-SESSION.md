Continuing @bto-labs/agy-bridge work from previous session.
Working directory: /home/jay/dev/util/agy-bridge
The package has been forked to bto-labs, renamed to @bto-labs/agy-bridge, and published as v0.5.0.

## Read these in order BEFORE doing anything else:

1. `docs/handoffs/2026-08-11-fork-and-publish.md`
   — Current state, what was changed, and the next steps the previous session left behind.
2. `CHANGELOG.md`
   — Canonical list of what shipped in v0.5.0.
3. `package.json`
   — Current package name, version, publish config, and entry points.

## Then:

Pick the highest-priority next action from the handoff (likely verifying `npm install @bto-labs/agy-bridge` works or adding a CI publish workflow) and run it.
